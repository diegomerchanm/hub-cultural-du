# requirements-web.txt — Hub Cultural DU
# Subconjunto liviano de requirements.txt, SOLO lo que necesita el
# dashboard (5_visualize_dashboard.py + pages/*.py + dash_common.py) para
# correr en producción. Deliberadamente NO incluye spaCy/torch/transformers/
# sentence-transformers/scikit-learn/igraph — esas dependencias son del
# pipeline de enriquecimiento (4_enrich_*.py), que corre aparte en GitHub
# Actions, nunca en el servidor que sirve el sitio. Mantiene el build de
# hosting rápido y dentro de los límites de memoria de un free tier.
#
# Uso (Render u otro hosting): pip install -r requirements-web.txt

dash==4.4.1
dash-bootstrap-components==2.0.4
dash-cytoscape==1.0.2
plotly==6.9.0
neo4j==6.2.0
python-dotenv==1.2.2
gunicorn==23.0.0
